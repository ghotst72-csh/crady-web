/** CRADY UI Polish 1.0 — the approved, restrained icon set. Exactly these
 * ten, used only at the specific call sites the spec named — never as
 * decoration on every heading. Adding a new icon here should be a
 * deliberate choice, not a default. */
export const ICON = {
  declaration: "📢",
  exDate: "📅",
  recordDate: "📒",
  payment: "💰",
  aiSummary: "🧠",
  aiConfidence: "🛡",
  expectedRange: "📊",
  predictionAccuracy: "🎯",
  risk: "⚠",
  cradyScore: "⭐",
} as const;
